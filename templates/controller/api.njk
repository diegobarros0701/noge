const { {{ serviceName }} } = require('../services/{{ serviceName }}');
const { NotFoundError, ValidationError } = require('objection');

module.exports = {
  {%- for action in actions %}
  {% include  "controller/actions/" + controllerActionsStyle + "/" + action + ".njk" ignore missing %}
  {{- "," if loop.index < (actions | length) }}
{% endfor %}

{{- '};' -}}