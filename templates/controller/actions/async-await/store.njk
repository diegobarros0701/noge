async store(req, res) {
    try {
      let {{ varName }} = await {{ modelName }}.query().insert(req.body);

      res.status(201).json({{ varName }});
    } catch(e) {
      res
        .status(error instanceof ValidationError ? 400 : 500)
        .json({
          error: true,
          message: e.message
        })
    }
  }